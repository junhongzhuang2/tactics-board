# 撤销 / 重做 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为战术板编辑加入撤销/重做：在 Zustand store 中以「引用快照」记录历史，撤销/重做跳回改动发生的帧与播放头，配工具栏按钮和键盘快捷键。

**Architecture:** store 新增 `past`/`future` 两个栈，每个条目保存对改动前 `board.data` 的**引用**（依赖现有 reducer 全程不可变更新，O(1) commit、结构共享）。6 个修改文档的动作经 `withHistory` 包裹记录历史；`undo`/`redo` 回滚并跳转视图。`markClean`/自动保存绝不记录历史（保住 redo）。快捷键判定抽成纯函数，按钮抽成独立展示组件，便于单测。

**Tech Stack:** React 18, Zustand 4, react-konva 18, Vite 5, Vitest + @testing-library/react（globals 已开，setup 在 `client/src/test-setup.js`，`vi` 全局可用）。

---

## File Structure

- **Modify** `client/src/store/boardStore.js` — 新增 `past`/`future` 状态、`snapshot`/`withHistory` 内部工具、`HISTORY_LIMIT` 常量；包裹 6 个修改动作；新增 `undo`/`redo`；`setBoard` 重置历史。
- **Modify** `client/src/store/boardStore.test.js` — 记录、撤销、重做、redo 失效、封顶、setBoard 清空、自动保存不污染、快照不可变 等测试；`beforeEach` 重置加 `past`/`future`。
- **Create** `client/src/utils/shortcuts.js` — `isUndoShortcut`/`isRedoShortcut` 纯函数。
- **Create** `client/src/utils/shortcuts.test.js` — 快捷键判定测试。
- **Create** `client/src/components/UndoRedoButtons.jsx` — 展示型按钮组件（props: canUndo, canRedo, onUndo, onRedo）。
- **Create** `client/src/components/UndoRedoButtons.test.jsx` — disabled 态与点击测试。
- **Modify** `client/src/components/BoardCanvas.jsx` — 接入按钮 + `keydown` 监听（焦点在 input 时放行）。

> 不需要数据库迁移：`past`/`future` 是运行时状态，不进 DB。

---

## Task 1: store 历史记录（state + withHistory + 包裹 6 个动作）

**Files:**
- Modify: `client/src/store/boardStore.js`
- Test: `client/src/store/boardStore.test.js`

- [ ] **Step 1: Write the failing tests**

(a) 在 `client/src/store/boardStore.test.js` 的 `beforeEach` 的 `setState` 中加入 `past: []` 和 `future: []`：
```js
beforeEach(() => {
  useBoardStore.setState({
    board: null,
    currentFrameIndex: 0,
    isDirty: false,
    isPlaying: false,
    playheadTime: 0,
    loop: false,
    past: [],
    future: [],
  })
})
```

(b) 在文件末尾追加：
```js
test('a mutating action pushes previous state to past and leaves future empty', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  expect(result.current.past.length).toBe(0)
  act(() => result.current.updateFramePlayerState(0, 'r1', { x: 0.3, y: 0.4, orientation: 0 }))
  expect(result.current.past.length).toBe(1)
  expect(result.current.future).toEqual([])
})

test('history caps at 200 entries', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  for (let i = 0; i < 201; i++) {
    act(() => result.current.updateFrameDiscState(0, { x: (i % 100) / 100, y: 0.5 }))
  }
  expect(result.current.past.length).toBe(200)
})

test('setBoard clears history', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  act(() => result.current.updateFrameDiscState(0, { x: 0.7, y: 0.2 }))
  expect(result.current.past.length).toBe(1)
  act(() => result.current.setBoard(makeBoard()))
  expect(result.current.past).toEqual([])
  expect(result.current.future).toEqual([])
})

test('history snapshot is not mutated by subsequent edits (immutability guard)', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard())) // frame0 r1.x = 0.1
  act(() => result.current.updateFramePlayerState(0, 'r1', { x: 0.5, y: 0.5, orientation: 0 }))
  expect(result.current.past[0].data.frames[0].playerStates.r1.x).toBe(0.1)
  act(() => result.current.updateFramePlayerState(0, 'r1', { x: 0.9, y: 0.9, orientation: 0 }))
  // 第一条快照仍指向最初状态，未被后续编辑污染
  expect(result.current.past[0].data.frames[0].playerStates.r1.x).toBe(0.1)
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd client; npx vitest run src/store/boardStore.test.js`
Expected: FAIL（`past` 为 undefined → length 报错 / 断言不通过）。

- [ ] **Step 3: Implement history recording in the store**

在 `client/src/store/boardStore.js` 顶部 `const MIN_DURATION = 100` 之后追加：
```js
const HISTORY_LIMIT = 200

// 快照保存对旧 board.data 的引用（依赖 reducer 全程不可变更新）
const snapshot = (s) => ({
  data: s.board.data,
  currentFrameIndex: s.currentFrameIndex,
  playheadTime: s.playheadTime,
})

// 在改动前压栈、清空重做栈，再合并本次改动
const withHistory = (s, next) => ({
  ...next,
  past: [...s.past, snapshot(s)].slice(-HISTORY_LIMIT),
  future: [],
})
```

在初始 state 中加入两个栈（放在 `loop: false,` 之后）：
```js
  past: [],
  future: [],
```

在 `setBoard` 的 set 对象中加入历史重置：
```js
  setBoard: (board) => set({
    board, currentFrameIndex: 0, isDirty: false,
    isPlaying: false, playheadTime: 0, loop: false,
    past: [], future: [],
  }),
```

把 6 个修改动作的返回值用 `withHistory(s, ...)` 包裹。逐个替换 return：

`updateFramePlayerState` 的 return 改为：
```js
    return withHistory(s, { board: { ...s.board, data: { ...s.board.data, frames } }, isDirty: true })
```

`updateFrameDiscState` 的 return 改为：
```js
    return withHistory(s, { board: { ...s.board, data: { ...s.board.data, frames } }, isDirty: true })
```

`insertFrameAfter` 的 return 改为：
```js
    return withHistory(s, {
      board: { ...s.board, data: { ...s.board.data, frames: next } },
      currentFrameIndex: newIndex,
      playheadTime: frameStartTimes(next)[newIndex],
      isPlaying: false,
      isDirty: true,
    })
```

`setFrameDuration` 的 return 改为：
```js
    return withHistory(s, { board: { ...s.board, data: { ...s.board.data, frames } }, isDirty: true })
```

`removeFrame`：保留开头的 `if (s.board.data.frames.length <= 1) return s`（不记历史），把后面的 return 改为：
```js
    return withHistory(s, {
      board: { ...s.board, data: { ...s.board.data, frames } },
      currentFrameIndex,
      playheadTime: frameStartTimes(frames)[currentFrameIndex],
      isPlaying: false,
      isDirty: true,
    })
```

`renamePlayer` 的 return 改为：
```js
    return withHistory(s, { board: { ...s.board, data: { ...s.board.data, players } }, isDirty: true })
```

> 不要包裹 `setCurrentFrame`/`setPlayhead`/`play`/`pause`/`toggleLoop`/`markClean`——它们不记历史。

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd client; npx vitest run src/store/boardStore.test.js`
Expected: PASS（含既有 store 测试，全部通过）。

- [ ] **Step 5: Commit**

```bash
git add client/src/store/boardStore.js client/src/store/boardStore.test.js
git commit -m "feat: record edit history in board store (reference snapshots)"
```
追加空行 + trailer：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Task 2: undo / redo 动作

**Files:**
- Modify: `client/src/store/boardStore.js`
- Test: `client/src/store/boardStore.test.js`

- [ ] **Step 1: Write the failing tests**

在 `client/src/store/boardStore.test.js` 末尾追加：
```js
test('undo restores previous data and moves current to future', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard())) // disc x = 0.5
  act(() => result.current.updateFrameDiscState(0, { x: 0.7, y: 0.2 }))
  expect(result.current.board.data.frames[0].discState.x).toBe(0.7)
  act(() => result.current.undo())
  expect(result.current.board.data.frames[0].discState.x).toBe(0.5)
  expect(result.current.past.length).toBe(0)
  expect(result.current.future.length).toBe(1)
})

test('redo reapplies an undone change and empties future', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  act(() => result.current.updateFrameDiscState(0, { x: 0.7, y: 0.2 }))
  act(() => result.current.undo())
  act(() => result.current.redo())
  expect(result.current.board.data.frames[0].discState.x).toBe(0.7)
  expect(result.current.future).toEqual([])
  expect(result.current.past.length).toBe(1)
})

test('a new edit after undo clears the redo stack', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  act(() => result.current.updateFrameDiscState(0, { x: 0.7, y: 0.2 }))
  act(() => result.current.undo())
  expect(result.current.future.length).toBe(1)
  act(() => result.current.updateFrameDiscState(0, { x: 0.3, y: 0.3 }))
  expect(result.current.future).toEqual([])
})

test('undo and redo are no-ops on empty stacks', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  act(() => result.current.undo())
  expect(result.current.past).toEqual([])
  expect(result.current.board.data.frames[0].discState.x).toBe(0.5)
  act(() => result.current.redo())
  expect(result.current.future).toEqual([])
})

test('undo restores the frame index and playhead where the edit happened', () => {
  const { result } = renderHook(() => useBoardStore())
  const board = makeBoard()
  board.data.frames.push({
    id: 'frame-1', duration: 500,
    playerStates: { r1: { x: 0.5, y: 0.5, orientation: 0 }, b1: { x: 0.5, y: 0.5, orientation: 0 } },
    discState: { x: 0.5, y: 0.5 }, annotations: [],
  })
  act(() => result.current.setBoard(board))
  act(() => result.current.setCurrentFrame(1)) // 导航到第1帧，playhead=1000（不记历史）
  act(() => result.current.updateFramePlayerState(1, 'r1', { x: 0.8, y: 0.8, orientation: 0 }))
  act(() => result.current.setCurrentFrame(0)) // 走开到第0帧
  act(() => result.current.undo())
  expect(result.current.currentFrameIndex).toBe(1) // 跳回改动处
  expect(result.current.playheadTime).toBe(1000)
  expect(result.current.board.data.frames[1].playerStates.r1.x).toBe(0.5) // 已回滚
})

test('markClean (autosave) does not pollute history; redo survives', () => {
  const { result } = renderHook(() => useBoardStore())
  act(() => result.current.setBoard(makeBoard()))
  act(() => result.current.updateFrameDiscState(0, { x: 0.7, y: 0.2 }))
  act(() => result.current.undo())
  expect(result.current.future.length).toBe(1)
  act(() => result.current.markClean()) // 模拟自动保存
  expect(result.current.future.length).toBe(1) // 未被污染
  act(() => result.current.redo())
  expect(result.current.board.data.frames[0].discState.x).toBe(0.7) // redo 仍可用
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd client; npx vitest run src/store/boardStore.test.js`
Expected: FAIL（`undo`/`redo` is not a function）。

- [ ] **Step 3: Implement undo / redo**

在 `client/src/store/boardStore.js` 的 `markClean` 之前插入：
```js
  undo: () => set((s) => {
    if (s.past.length === 0) return s
    const prev = s.past[s.past.length - 1]
    const cur = snapshot(s)
    return {
      board: { ...s.board, data: prev.data },
      currentFrameIndex: prev.currentFrameIndex,
      playheadTime: prev.playheadTime,
      isPlaying: false,
      isDirty: true,
      past: s.past.slice(0, -1),
      future: [...s.future, cur],
    }
  }),

  redo: () => set((s) => {
    if (s.future.length === 0) return s
    const entry = s.future[s.future.length - 1]
    const cur = snapshot(s)
    return {
      board: { ...s.board, data: entry.data },
      currentFrameIndex: entry.currentFrameIndex,
      playheadTime: entry.playheadTime,
      isPlaying: false,
      isDirty: true,
      past: [...s.past, cur],
      future: s.future.slice(0, -1),
    }
  }),
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd client; npx vitest run src/store/boardStore.test.js`
Expected: PASS（全部通过）。

- [ ] **Step 5: Commit**

```bash
git add client/src/store/boardStore.js client/src/store/boardStore.test.js
git commit -m "feat: undo and redo actions with view restore, autosave-safe"
```
追加空行 + trailer：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Task 3: 快捷键判定纯函数

**Files:**
- Create: `client/src/utils/shortcuts.js`
- Test: `client/src/utils/shortcuts.test.js`

- [ ] **Step 1: Write the failing test**

写入 `client/src/utils/shortcuts.test.js`：
```js
import { isUndoShortcut, isRedoShortcut } from './shortcuts'

test('isUndoShortcut matches Ctrl+Z and Cmd+Z', () => {
  expect(isUndoShortcut({ ctrlKey: true, shiftKey: false, key: 'z' })).toBe(true)
  expect(isUndoShortcut({ metaKey: true, shiftKey: false, key: 'z' })).toBe(true)
  expect(isUndoShortcut({ ctrlKey: true, shiftKey: false, key: 'Z' })).toBe(true)
})

test('isUndoShortcut rejects Ctrl+Shift+Z and bare z', () => {
  expect(isUndoShortcut({ ctrlKey: true, shiftKey: true, key: 'z' })).toBe(false)
  expect(isUndoShortcut({ key: 'z' })).toBe(false)
})

test('isRedoShortcut matches Ctrl+Shift+Z, Cmd+Shift+Z and Ctrl+Y', () => {
  expect(isRedoShortcut({ ctrlKey: true, shiftKey: true, key: 'z' })).toBe(true)
  expect(isRedoShortcut({ metaKey: true, shiftKey: true, key: 'z' })).toBe(true)
  expect(isRedoShortcut({ ctrlKey: true, key: 'y' })).toBe(true)
})

test('isRedoShortcut rejects plain Ctrl+Z and bare y', () => {
  expect(isRedoShortcut({ ctrlKey: true, shiftKey: false, key: 'z' })).toBe(false)
  expect(isRedoShortcut({ key: 'y' })).toBe(false)
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd client; npx vitest run src/utils/shortcuts.test.js`
Expected: FAIL（找不到 `./shortcuts`）。

- [ ] **Step 3: Write the implementation**

写入 `client/src/utils/shortcuts.js`：
```js
// Ctrl/Cmd + Z（不含 Shift）= 撤销
export function isUndoShortcut(e) {
  return (e.ctrlKey || e.metaKey) && !e.shiftKey && (e.key === 'z' || e.key === 'Z')
}

// Ctrl/Cmd + Shift + Z，或 Ctrl/Cmd + Y = 重做
export function isRedoShortcut(e) {
  return (e.ctrlKey || e.metaKey) && (
    ((e.key === 'z' || e.key === 'Z') && e.shiftKey) ||
    (e.key === 'y' || e.key === 'Y')
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd client; npx vitest run src/utils/shortcuts.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add client/src/utils/shortcuts.js client/src/utils/shortcuts.test.js
git commit -m "feat: undo/redo keyboard shortcut matchers"
```
追加空行 + trailer：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Task 4: UndoRedoButtons 展示组件

**Files:**
- Create: `client/src/components/UndoRedoButtons.jsx`
- Test: `client/src/components/UndoRedoButtons.test.jsx`

- [ ] **Step 1: Write the failing test**

写入 `client/src/components/UndoRedoButtons.test.jsx`：
```js
import { render, screen, fireEvent } from '@testing-library/react'
import UndoRedoButtons from './UndoRedoButtons'

test('buttons are disabled when canUndo/canRedo are false', () => {
  render(<UndoRedoButtons canUndo={false} canRedo={false} onUndo={vi.fn()} onRedo={vi.fn()} />)
  expect(screen.getByLabelText('撤销')).toBeDisabled()
  expect(screen.getByLabelText('重做')).toBeDisabled()
})

test('clicking enabled buttons calls handlers', () => {
  const onUndo = vi.fn()
  const onRedo = vi.fn()
  render(<UndoRedoButtons canUndo={true} canRedo={true} onUndo={onUndo} onRedo={onRedo} />)
  fireEvent.click(screen.getByLabelText('撤销'))
  fireEvent.click(screen.getByLabelText('重做'))
  expect(onUndo).toHaveBeenCalledTimes(1)
  expect(onRedo).toHaveBeenCalledTimes(1)
})

test('clicking a disabled button does not call its handler', () => {
  const onUndo = vi.fn()
  render(<UndoRedoButtons canUndo={false} canRedo={true} onUndo={onUndo} onRedo={vi.fn()} />)
  fireEvent.click(screen.getByLabelText('撤销'))
  expect(onUndo).not.toHaveBeenCalled()
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd client; npx vitest run src/components/UndoRedoButtons.test.jsx`
Expected: FAIL（找不到 `./UndoRedoButtons`）。

- [ ] **Step 3: Write the component**

写入 `client/src/components/UndoRedoButtons.jsx`：
```js
const btnStyle = (enabled) => ({
  padding: '0 10px', height: 28, borderRadius: 6,
  background: '#2a2a3e', border: '1px solid #555',
  color: enabled ? '#ccc' : '#555',
  cursor: enabled ? 'pointer' : 'default',
  fontSize: 14, lineHeight: 1,
})

export default function UndoRedoButtons({ canUndo, canRedo, onUndo, onRedo }) {
  return (
    <>
      <button style={btnStyle(canUndo)} aria-label="撤销" disabled={!canUndo} onClick={onUndo}>↶</button>
      <button style={btnStyle(canRedo)} aria-label="重做" disabled={!canRedo} onClick={onRedo}>↷</button>
    </>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd client; npx vitest run src/components/UndoRedoButtons.test.jsx`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add client/src/components/UndoRedoButtons.jsx client/src/components/UndoRedoButtons.test.jsx
git commit -m "feat: UndoRedoButtons presentational component"
```
追加空行 + trailer：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Task 5: 接入 BoardCanvas（按钮 + 键盘监听）

**Files:**
- Modify: `client/src/components/BoardCanvas.jsx`

> 键盘监听是 store + window 的胶水层，焦点判定逻辑由 Task 3 的纯函数覆盖测试；此处不写组件级自动化测试，由人工浏览器验证。

- [ ] **Step 1: Add imports**

在 `client/src/components/BoardCanvas.jsx` 顶部 import 段加入：
```js
import UndoRedoButtons from './UndoRedoButtons'
import { isUndoShortcut, isRedoShortcut } from '../utils/shortcuts'
```

- [ ] **Step 2: Destructure new store fields**

把 store 解构（`const { ... } = useBoardStore()`）扩展为包含 `past`、`future`、`undo`、`redo`：
```js
  const {
    board, currentFrameIndex, isDirty, playheadTime, isPlaying, loop,
    past, future, undo, redo,
    updateFramePlayerState, updateFrameDiscState,
    insertFrameAfter, removeFrame, setCurrentFrame, setFrameDuration,
    setPlayhead, play, pause, toggleLoop, markClean,
  } = useBoardStore()
```

- [ ] **Step 3: Add the keyboard listener effect**

在 `usePlaybackEngine()` 调用之后、自动保存 `useEffect` 之前，加入：
```js
  // 撤销/重做快捷键；焦点在输入框时放行给浏览器原生文本撤销
  useEffect(() => {
    function onKeyDown(e) {
      const tag = e.target?.tagName
      if (tag === 'INPUT' || tag === 'TEXTAREA') return
      if (isUndoShortcut(e)) { e.preventDefault(); undo() }
      else if (isRedoShortcut(e)) { e.preventDefault(); redo() }
    }
    window.addEventListener('keydown', onKeyDown)
    return () => window.removeEventListener('keydown', onKeyDown)
  }, [undo, redo])
```

- [ ] **Step 4: Render the buttons in the top bar**

把顶栏里板名 span 之后加入按钮（在 `{isDirty && ...}` 之前）：
```js
        <span style={{ fontWeight: 'bold', fontSize: 16 }}>{board?.name ?? '加载中…'}</span>
        {board && (
          <UndoRedoButtons
            canUndo={past.length > 0}
            canRedo={future.length > 0}
            onUndo={undo}
            onRedo={redo}
          />
        )}
        {isDirty && <span style={{ fontSize: 12, color: '#888' }}>保存中…</span>}
```

- [ ] **Step 5: Run full suite + build**

Run: `cd client; npx vitest run`
Expected: PASS（所有测试通过，无回归）。
Run: `cd client; npx vite build`
Expected: 构建成功，无未用导入/语法错误。

- [ ] **Step 6: Manual smoke test (browser)**

启动前后端，打开一个战术板：
- 拖球员 → 点撤销按钮：回到拖之前；点重做：恢复。
- 按 `Ctrl+Z` 撤销、`Ctrl+Shift+Z` 与 `Ctrl+Y` 重做。
- 在不同帧编辑后走到别的帧，按撤销：画面**跳回**改动那一帧。
- 撤销若干步后等 ~1 秒（自动保存触发），再按重做：**仍能正常重做**（验证自动保存不杀 redo）。
- 焦点放进时长输入框，按 `Ctrl+Z`：撤销的是输入框文本，**不**影响战术板。
- 无可撤销/重做时按钮置灰。

> 把结果反馈给我，尤其自动保存后 redo 是否仍可用、输入框里 Ctrl+Z 是否不误伤。

- [ ] **Step 7: Commit**

```bash
git add client/src/components/BoardCanvas.jsx
git commit -m "feat: wire undo/redo buttons and keyboard shortcuts into BoardCanvas"
```
追加空行 + trailer：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

---

## Self-Review（已执行）

**1. Spec coverage（对照设计文档逐节）：**
- §3 状态/数据结构（past/future、HistoryEntry 引用、HISTORY_LIMIT、setBoard 重置）→ Task 1 ✅
- §4 记录机制（snapshot/withHistory、6 记录动作、5 不记录动作）→ Task 1 ✅
- §4 undo/redo（回滚 data+帧+播放头、暂停、isDirty、空栈 no-op、future 清空）→ Task 2 ✅
- §3/§7 不变量「快照不被后续编辑污染」→ Task 1 immutability guard 测试 ✅
- §5 按钮（置灰/aria-label）→ Task 4 ✅；快捷键纯函数 → Task 3 ✅；keydown 监听 + 输入框放行 → Task 5 ✅
- §6 自动保存不污染历史（回归测试）→ Task 2 markClean 测试 ✅
- §6 播放与历史无关 → 设计上 play/pause 不记录（Task 1 未包裹）+ undo/redo 置 isPlaying:false（Task 2）✅
- §7 历史封顶 200、setBoard 清空 → Task 1 ✅

**2. Placeholder scan：** 无 TODO/TBD；每个代码步骤均含完整代码。

**3. Type consistency：** `snapshot`/`withHistory`/`HISTORY_LIMIT` 在 Task 1 定义、Task 2 复用一致；`past`/`future`/`undo`/`redo` 在 Task 1-2 定义、Task 5 消费一致；`isUndoShortcut`/`isRedoShortcut` 在 Task 3 定义、Task 5 调用一致；`UndoRedoButtons` props（canUndo/canRedo/onUndo/onRedo）在 Task 4 定义、Task 5 传入一致。

**已知取舍：** keydown 监听的接线（window 事件、输入框放行）不写组件级自动化测试——焦点判定的核心由 Task 3 纯函数覆盖，接线由 Task 5 Step 6 人工浏览器验证。
